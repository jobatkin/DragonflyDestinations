import CachedIcon from '@mui/icons-material/Cached';

// auto-rendered whenever using the useSearchParams hook in the build phase
export default function Loading() {
    return (
        <div><CachedIcon /> Loading ...</div>
    )
}